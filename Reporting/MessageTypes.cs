namespace NUnitReporter.Reporting
{
    /// <summary>
    /// A set of message types which can be taken into account by some connected reporter helpers during reports generation.
    /// </summary>
    public enum MessageTypes { Standard, Skipped, Failed, ActionTitle, Notify};
}